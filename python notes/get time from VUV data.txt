# define a function to convert the string with colons into a number

def get_sec(time_str):
    """Get Seconds from time."""
    h, m, s, =time_str.split(':')
    return int(h)*3600+int(m)*60+int(s)

#Define an empty array for all time stamps
timestamp_raw = np.array([])

#Use the function above to extract the time to your array

a=float(get_sec(time[1]))
    timestamp_raw=np.append(timestamp_raw,(a))
